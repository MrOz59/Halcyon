# Requests for Comments

RFCs are design proposals that have not yet become accepted architecture.

A substantial subsystem should begin as an RFC when it introduces new protocol behavior, persistence formats, extension APIs, security boundaries, or cross-cutting architecture.

## Lifecycle

```text
Draft -> Discussion -> Proposed -> Accepted/Rejected
```

Accepted RFCs should produce one or more ADRs and corresponding HTDS updates.
